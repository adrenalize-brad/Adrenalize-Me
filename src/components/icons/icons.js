import React from 'react'

const FacebookIcon = ({className}) => {
    return(
    <i className={`fab fa-facebook-f ${className}`}></i>
    )
};

const FbMessengerIcon = ({className}) => {
    return(
    <i className={`fab fa-facebook-messenger ${className}`}></i>
    )
};

const LinkedInIcon = ({className}) => {
    return(
    <i className={`fab fa-linkedin-in ${className}`}></i>
    )
};

const TwitterIcon = ({className}) => {
    return(
    <i className={`fab fa-twitter ${className}`}></i>
    )
};

const PinterestIcon = ({className}) => {
    return(
    <i className={`fab fa-pinterest-p ${className}`}></i>
    )
};

const RedditIcon = ({className}) => {
    return(
    <i className={`fab fa-reddit-alien ${className}`}></i>
    )
};

const WhatsappIcon = ({className}) => {
    return(
    <i className={`fab fa-whatsapp ${className}`}></i>
    )
};

const EmailIcon = ({className}) => {
    return(
    <i className={`fas fa-envelope ${className}`}></i>
    )
};

const SearchIcon = ({className}) => {
    return(
    <i className={`fas fa-search ${className}`}></i>
    )
};

const CloseIcon = ({className}) => {
    return(
    <i className={`fas fa-times-circle ${className}`}></i>
    )
};

const ContactIcon = ({className}) => {
    return(
        <i className={`fas fa-envelope ${className}`}></i>
    )
};
    

export { FacebookIcon, FbMessengerIcon, LinkedInIcon, TwitterIcon, PinterestIcon, RedditIcon, WhatsappIcon, EmailIcon, SearchIcon, CloseIcon, ContactIcon };